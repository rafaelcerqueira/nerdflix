import styled from 'styled-components';

const LinkNerdflix = styled.a `
    text-decoration: none;
    font-weight: bold;
    color: var(--color-blue-sky);
    cursor: pointer;
`;

export default LinkNerdflix;